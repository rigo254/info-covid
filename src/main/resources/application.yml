# Server properties
server:
  servlet:
    context-path: /info-covid-api
  port: 8080
# Spring properties
spring:
  # Dev is active by default. This value can be overwritten by java arg param
  profiles:
    active: heroku
  application:
    name: info-covid-api